# one-serverless-basic-typescript

Serverless Framework using esbuild -> typescript.

### Serverless plugins
- serverless-esbuild
- serverless-offline

### Set ENV variables
```shell
cp .env.example .env
```
